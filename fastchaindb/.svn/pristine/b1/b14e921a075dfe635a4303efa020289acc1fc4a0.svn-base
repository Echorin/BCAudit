package com.oschain.fastchaindb.fabric.dto;

import org.hyperledger.fabric.sdk.BlockEvent;
import org.hyperledger.fabric.sdk.ProposalResponse;

import java.util.Collection;

public class TransactionResult {

    BlockEvent.TransactionEvent transactionEvent;
    Collection<ProposalResponse>  proposalResponse;

    public BlockEvent.TransactionEvent getTransactionEvent() {
        return transactionEvent;
    }

    public void setTransactionEvent(BlockEvent.TransactionEvent transactionEvent) {
        this.transactionEvent = transactionEvent;
    }

    public Collection<ProposalResponse> getProposalResponse() {
        return proposalResponse;
    }

    public void setProposalResponse(Collection<ProposalResponse> proposalResponse) {
        this.proposalResponse = proposalResponse;
    }

    public static void main(String[] args) {

//        ConcurrentLinkedQueue<String> concurrentLinkedQueue = new ConcurrentLinkedQueue<String>();
//        concurrentLinkedQueue.offer(proposalResponse);
//        concurrentLinkedQueue.offer("b");
//        concurrentLinkedQueue.offer("c");
//        concurrentLinkedQueue.offer("d"); // 将指定元素插入到此队列的尾部。
        //concurrentLinkedQueue.peek(); // 检索并移除此队列的头，如果此队列为空，则返回 null。

//        System.out.println(concurrentLinkedQueue.poll()); // 检索并移除此队列的头，如果此队列为空，则返回 null。

//        for (String str : concurrentLinkedQueue) {
//            System.out.println(str);
//        }
    }
}
